name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.2.0). Must already exist or be created by this dispatch.'
        required: true

jobs:
  release:
    uses: RemoteMedia-SDK/remotemedia-sdk/.github/workflows/build-plugin.yml@main
    with:
      crate-name:   echo-python-loadable-plugin
      display-name: echo-python-loadable
      tag:          ${{ inputs.tag || github.ref_name }}
    permissions:
      contents: write
